const colors = {
    blue: '#0000FF',
    mediumBlue: '#0000CD',
    skyBlue: '#87CEEB',
    lightSkyBlue: '#87CEFA',
    royalBlue: '#4169E1',
    slateBlue: '#6A5ACD',
    mediumSlateBlue: '#7B68EE',
    black: '#000000',
    white: '#FFFFFF',
    deepPink: '#FF1493',
    red: '#FF0000',
    gray: '#808080',
    darkGray: '#A9A9A9',
    silver: '#C0C0C0',
    lightGray: '#D3D3D3',
    purple: '#9400D3',
    darkOrchid: '#9932CC',
    rebeccaPurple: '#663399',
    mediumPurple: '#9370DB',
    violet: '#8A2BE2',
    darkSlateBlue: '#483D8B',
    darkBlue: '#00008B',
    indigo: '#4B0082',
    plum: '#DDA0DD',
    lavender: '#E6E6FA',
    thistle: '#D8BFD8',
    mediumOrchid: '#BA55D3',
}

const margins = {
    smallMargin: 5,
    mediumMargin: 10,
    largeMargin: 15,
    extraLargeMargin: 20,
}

const paddings = {
    smallPadding: 5,
    mediumPadding: 10,
    largePadding: 15,
    extraLargePadding: 20,
}

const borderRadius = {
    smallBorderRadius: 7,
    mediumBorderRadius: 10,
    largeBorderRadius: 15,
    extraLargeBorderRadius: 20,
}

const font = {
    bold : 'bold',
    italic : 'italic',
    normal : 'normal',
    underline : 'underline',
    lineThrough : 'line-through',
}

const fontSize = {
    smallFontSize: 10,
    mediumFontSize: 15,
    largeFontSize: 20,
    extraLargeFontSize: 25,
}

const borderWidth = {
    extraSmallBorderWidth: 0.5,
    smallBorderWidth: 1,
    mediumBorderWidth: 2,
}

const alignment = {
    absolute: 'absolute',
    left: 'left',
    right: 'right',
    center: 'center',
    auto: 'auto',
}

const flexAlign = {
    start: 'flex-start',
    end: 'flex-end',
    center: 'center',
    spaceBetween: 'spaceBetween',
    spaceAround: 'space-around',
}

const flexDirection = {
    row: 'row',
    column: 'column',
}

const length = {
    full: '100%',
    threeQuarter: '75%',
    half: '50%',
    quarter: '25%',
    auto: 'auto',
}

export default {
    colors,
    margins,
    paddings,
    borderRadius,
    font,
    fontSize,
    borderWidth,
    alignment,
    flexAlign,
    flexDirection,
    length,
}